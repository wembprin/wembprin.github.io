    <div class="card h-100 hoverable">
        {% if member.image %}
            {%
                include figure.liquid
                loading="eager"
                path=member.image
                sizes="250px"
                alt=member.name
                class="card-img-top"
            %}
        {% endif %}
        <div class="card-body">
            <h6 class="card-title"> <a href="{{ member.website }}"> {{ member.name }} </a></h6>
            <div class="d-flex justify-content-between align-items-center">
                <span class="card-text">{{ member.affiliation }}</span>
                {% comment %} <div>
                    <a href="{{ member.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
                    <a href="{{ member.scholar }}" class="ml-2"><i class="fa-solid fa-magnifying-glass"></i></a>
                </div> {% endcomment %}
            </div>
        </div>
    </div>